# Roadmap

The v2.1.0 theorem interface is intended to remain stable. Future work must be
versioned and must not silently expand the current claims.

1. Record a green clean-cache Lean CI run and archive DOI for v2.1.0.
2. Decide whether to adopt the proposed repository name
   `lean-rooted-tree-polymer-expansion`.
3. Add a publisher-specific renderer generated from the canonical Markdown only
   if a venue requires one.
4. Formalize a sharper tree-profile constant if a genuine Catalan or restricted
   incompatibility enumeration becomes available.
5. Instantiate the weight split from primary Balaban/Dimock source statements.
6. Prove the concrete raw gauge activity estimate in the upstream Yang--Mills
   analysis programme.
7. Generalize the finite tree engine to other target-preserving polymer
   geometries.

No roadmap item is part of the theorem claimed in v2.1.0.
